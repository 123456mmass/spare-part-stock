{
  "name": "spare-part-stock",
  "version": "1.0.0",
  "description": "CLI to install/update the Spare Part Stock Next.js app on a server. The app code lives in the git repo; this CLI wraps git pull + build + pm2 restart. Per-instance data (.env, dev.db, uploads) is never shipped.",
  "license": "MIT",
  "bin": {
    "spare-part-stock": "./index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "index.js"
  ]
}
